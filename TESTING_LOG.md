# 📜 Log de Execução — Histórico de Testes

> Histórico append-only de quem mexeu no quê. **Separado de TESTING.md** para não inflar o contexto das IAs (o CLAUDE.md importa TESTING.md inteiro toda sessão).
> A POLÍTICA de testes está em TESTING.md — leia ela antes de qualquer alteração.

Formato: `- [DATA] [MODELO] — [O QUE FEZ] — [QUANTOS TESTES NOVOS] — [STATUS DA SUITE]`

<!-- AS IAs DEVEM ANEXAR LINHAS NO FINAL DESTE ARQUIVO APÓS CADA TAREFA -->

- 2026-05-20 Claude Opus 4.7 (1M) — Criou TESTING.md + arquivos de regra para 7 IAs + testes de `normalizar`, `loadAll` e `kmFlow` — +3 arquivos de teste, ~40 casos novos — ✅ 191/191 passando (16 arquivos)
- 2026-05-20 Gemini 3.5 Flash (High) / Antigravity — Corrigiu erros de compilação TS em `kmFlow.test.ts` e validou a suíte inteira — 0 novos testes — ✅ 191/191 passando (16 arquivos)
- 2026-05-20 Gemini 3.5 Flash (High) / Antigravity — Corrigiu erros de sintaxe JSX/JS em `AcertoMensalTab.tsx` e validou com tsc e vitest — 0 novos testes — ✅ 191/191 passando (16 arquivos)
- 2026-05-20 Gemini 3.5 Flash (High) / Antigravity — Adicionou ordenação interativa de Fretes por Rota, Data Fim e Saldo Restante usando Th do ds.tsx — 0 novos testes — ✅ 191/191 passando (16 arquivos)
- 2026-05-21 Claude Opus 4.7 (1M) — Limpeza ETAPA 2 do `PLANO_LIMPEZA_MODELO.md`: refactor completo viagens→pedidos, fretes→entregas, remoção de comissão, despesas→veiculo_id, frete_id removido de tabelas auxiliares, datas de pedidos renomeadas. Atualizou ~25 arquivos TS/TSX, fixed kmFlow tests (`km_registrado`→`km_lido`) — +1 teste novo (valor_diaria_por_pedido em motorista schema) — ✅ 192/192 passando (16 arquivos), `tsc --noEmit` 0 erros
- 2026-05-27 Claude Sonnet 4.6 (Thinking) / Antigravity — Migração completa Meta Cloud API → Evolution API: reescrita de `security.ts`, `messageParser.ts`, `messageSender.ts`, `webhook/route.ts` + testes correspondentes. Todos os 8 flows, router, auth e sessionManager preservados sem alteração — +0 testes novos de lógica, todos os testes de infra WhatsApp reescritos para o novo provider — ✅ 189/189 passando (16 arquivos)
- 2026-05-27 Gemini 3.5 Flash (High) / Antigravity — Resolução de problemas de infra, deploy e SSL na Evolution API do Railway, persistência Postgres ativada com sslmode=no-verify e salvando instâncias com sucesso. Validou a suite de testes locais — 0 novos testes — ✅ 189/189 passando (16 arquivos)
- 2026-05-27 Gemini 3.5 Flash (Medium) / Antigravity — Corrigido parser de JID alternativo da Evolution API para resolver LID com terminação @s.whatsapp.net e restaurar recebimento de mensagens do bot — +1 teste novo (LID com @s.whatsapp.net) — ✅ 195/195 passando (16 arquivos)
- 2026-05-28 Claude Opus 4.7 (1M) — **MVP de Roteirização — Fase 0 + Fase 1 completas** (PLANO_ROTEIRIZACAO.md): Setup completo (5 tabelas Supabase, deps npm, env vars, types), ViaCEP+cache (1.1), fila offline Dexie+sync+API (1.2), InputEnderecoNF+API+browser client (1.3), tela mobile captura-notas (1.4), Nominatim geocoding (1.5), API geocodar (1.6), cliente OSRM (1.7), cliente VROOM (1.8), helpers de restrições VROOM (1.9), API otimizar (1.10), MapaRota Leaflet+polyline decoder (1.11), tela Ajuste de Rota com tabs/dnd-kit/modal (1.12 + 2 sub-APIs), deep links Waze/Google Maps (1.13), utilitário estimarRota (1.14). Polish adiado: animações de pinos, modal de impacto km/min, cliente_preferencias, integração visual com entregas/novo. — **+257 testes novos em 24 arquivos novos** — ✅ 452/452 passando (40 arquivos)
- 2026-05-29 Gemini 3.5 Flash (High) / Antigravity — Resolvido bug que impedia visualizar outros usuários (RLS de usuario_empresas corrigido) + RLS bypass seguro nas Server Actions de edição/remoção de usuários + Refactor RemoverUsuarioBtn para server action — +6 testes novos em 1 arquivo novo — ✅ 488/488 passando (45 arquivos)
- 2026-05-29 Gemini 3.5 Flash (High) / Antigravity — Resolvido bug de login multidomínio (novo @frota.sys + legado @ronaldoborba.com.br) + Feedback visual de erro com banner na UI e tratamento de useSearchParams em Suspense — +4 testes novos em 1 arquivo novo — ✅ 493/493 passando (46 arquivos)
- 2026-05-29 Claude Opus 4.6 (Thinking) / Antigravity — Fix reordenação paradas (CHECK constraint), z-index Leaflet sobre modais (isolation stacking context), auto-refresh da rota ao voltar de ajuste-rota (visibilitychange + pageshow) — +1 teste novo — ✅ 518/518 passando (47 arquivos)
- 2026-05-30 Claude Opus 4.7 (1M) — **Bot WhatsApp: pipeline áudio Deepgram → Gemini** (resolveu bug de Gemini retornar respostas genéricas com OGG/Opus do WhatsApp). Reescreveu `chatGeminiComAudio` pra: 1) transcrever áudio via Deepgram nova-2 pt-BR, 2) enviar texto pro `chatGemini` text-only com histórico. `processarAudioComGemini` agora grava a transcrição real no histórico (não mais "(mensagem de voz)") + mensagem específica pra áudio inaudível. +18 testes novos em 3 arquivos (deepgramClient, geminiClient áudio, geminiBot) — ✅ 588/588 passando (56 arquivos)
- 2026-05-30 Claude Opus 4.7 (1M) — **Validação de endereços via Overpass self-hosted**: lib completa (`src/lib/routing/overpass/`) + API route + integração UI. Bbox query (sem dependência de areas pre-geradas) + cache Supabase com TTL adaptativo (30d positivo, 14d baixa confiança, 6m sem_dados) + badge visual no `InputEnderecoNF` (🟢confirmado / 🟡plausível / 🟠suspeito) sem bloquear o fluxo. 4 agentes de pesquisa convergiram na estratégia. +37 testes em 5 arquivos novos — ✅ 627/627 passando (61 arquivos)
- 2026-05-30 Claude Opus 4.7 (1M) — **BOT_FRAMEWORK.md** consolidado de 7 agentes (5 pesquisa + 2 código). Documento de 495 linhas: arquitetura em 8 camadas, 11 bugs identificados com severidade, regras pra tools/prompt/erro, Permission Loop pra ações destrutivas, zona protegida, roadmap em 5 fases. Sem código novo — apenas planejamento — ✅ 627/627 passando
- 2026-05-30 Claude Opus 4.7 (1M) — **Fase 1 do BOT_FRAMEWORK executada (4 bugs críticos corrigidos)**: B1 histórico migrado pra Supabase (`whatsapp_historico` table + `src/lib/whatsapp/historico.ts` com fallback gracioso), B2 validação rigorosa de NaN/Infinity/decimal em propor+confirmar KM, B3 error handling em messageRouter (3 queries silenciosas tratadas), B4 Permission Loop implementado (`propor_atualizacao_km` read-only + `confirmar_atualizacao_km` executa, com legacy redirect anti-regressão). SYSTEM_PROMPT reescrito (template §5.3 do framework). Migration SQL em `db/migration_whatsapp_historico.sql`. +36 testes (historico 8, geminiBot atualizado 8, frotaTools 13, dispatcher 7) — ✅ 663/663 passando (63 arquivos)
- 2026-05-29 Gemini 3.1 Pro (Low) / Antigravity — Implementação de captura de endereço por voz (Speech-to-Text) usando Web Speech API nativa. Botão microfone adicionado no InputEnderecoNF com extração inteligente de CEP e fallback para geocoding completo do Nominatim — +10 testes novos em 2 arquivos novos — ✅ 528/528 passando (49 arquivos)
- 2026-05-29 Claude Sonnet 4.6 (Thinking) / Antigravity — Histórico de rotas na tela de Início. Motorista vê as últimas 5 rotas ao abrir "Rota do Dia". Rotas em aberto aparecem em vermelho com aviso. Pode retomar ou criar nova. Refatoração geral dos testes de fase em_rota para seguir o novo fluxo de navegação — +3 novos testes — ✅ 531/531 passando (49 arquivos)
- 2026-05-29 Gemini 2.5 Pro / Antigravity — Múltiplos resultados de geocoding ao falar endereço: `geocodarMultiplos` + `calcularDistanciaKm` em geocoding.ts, GET em `/api/routing/geocodar`, componente `ListaOpcoesEndereco`, nova etapa `escolha_endereco` em `InputEnderecoNF` com geolocalização GPS para ordenar por proximidade. Edição de NF capturada: `editarNota` em fila.ts, botão ✏️ e modo edição em `FaseCaptura` — +22 testes novos em 3 arquivos novos — ✅ 558/558 passando (52 arquivos)
- 2026-05-29 Gemini 3.5 Flash (High) / Antigravity — Criou método PATCH em /api/routing/rota/[id] para atualização do status da rota no banco de dados e integrou na ação 'Encerrar rota' do motorista — +5 testes novos — ✅ 563/563 passando (52 arquivos)
- 2026-05-29 Gemini 3.5 Flash (High) / Antigravity — Criou endpoint POST /api/routing/notas/limpar e limparFila no Dexie para zerar totalmente a fila offline e as notas capturadas pendentes no Supabase ao iniciar uma nova rota — +5 testes novos em 2 arquivos novos — ✅ 568/568 passando (54 arquivos)
- 2026-05-29 Gemini 2.5 Pro / Antigravity — Fallback progressivo no geocoding: quando Nominatim não encontra com query completa (logradouro+numero+bairro+cidade+uf+cep), tenta automaticamente sem bairro/CEP, só com número, e por fim só logradouro+cidade+uf. Resolve endereços brasileiros que o Nominatim público não encontra na busca completa — +2 testes novos — ✅ 570/570 passando (53 arquivos)
- 2026-05-30 Gemini 2.5 Flash (Claude Thinking) / Antigravity — Novas tools do Gemini para consultar e atualizar KM do caminhão via WhatsApp: `buscar_km_caminhao` (busca via km_logs + fallback pedido ativo) e `atualizar_km_caminhao` (insere em km_logs, trigger propaga para veiculos.km_atual). Pipeline completo text+audio com motoristaId injetado do auth. SYSTEM_PROMPT atualizado com instruções das novas tools. — +9 testes novos em 1 arquivo novo (`frotaTools.test.ts`) — ✅ 638/638 passando (62 arquivos)
- 2026-05-29 Claude Opus 4.7 (1M) — **BOT_FRAMEWORK Fase 2 + bugs restantes (B6/B8/B9)**: (B9) `comRetry` centralizado em `src/lib/ai/retry.ts` com backoff exponencial — só retenta em 5xx/429/network, nunca 4xx — wrappeando chamadas do Gemini e Deepgram. (B6) Multi-turn tool loop no `geminiClient` com cap `MAX_TOOL_ROUNDS=5` (evita loops infinitos). (B8) `prefixarComRemetente` extraído pra `src/lib/ai/contexto.ts` (fim do drift entre geminiClient/geminiBot). **Fase 2**: fast-path regex em `src/lib/whatsapp/fastPath.ts` (saudação/ajuda/encerramento/reset) bypassa o LLM em mensagens curtas; métricas estruturadas em `bot_metricas` table (`src/lib/ai/metricas.ts` + `db/migration_bot_metricas.sql`) com fire-and-forget pra capturar tokens, tools, latência, cached_tokens. Tests de fast-path/retry/geminiBot atualizados, e2e/messageRouter mudados pra perguntas reais (fast-path antes do mock do Gemini). — +20 testes novos em 2 arquivos novos (retry, fastPath) — ✅ 683/683 passando (65 arquivos)
- 2026-05-30 Claude Opus 4.7 (1M) — **Fix: bot "burro" pra perguntas tipo "quanto km tem o leão" e "qual meu caminhão"**. `buscar_km_caminhao` agora aceita `placa_ou_apelido?` opcional (busca por placa OU apelido case-insensitive na empresa, com erro descritivo quando ambíguo ou nao encontrado). Nova tool `meu_caminhao` pra "qual veiculo esta vinculado a mim". SYSTEM_PROMPT atualizado com mapeamento explicito dos casos. +8 testes novos em frotaTools.test.ts (modo apelido, ambiguidade, meu_caminhao, dispatcher). — ✅ 691/691 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.7 (1M) — **Fix bug critico: "First content should be with role 'user', got model"** — Gemini rejeitava 100% das chamadas (texto + audio) quando historico iniciava com role 'model'. Causa raiz: `void gravarMensagem(user) + void gravarMensagem(model)` em paralelo → Postgres invertia ordem de `created_at` em race condition. Janela movel de 8 msgs as vezes pegava um 'model' orfa do turno anterior como primeira. **Fix duplo**: (a) gravacao SEQUENCIAL com await em geminiBot.ts (texto + audio) garante monotonicidade de timestamp; (b) defesa em lerHistorico — descarta msgs 'model' do inicio antes de devolver (resiliencia contra dados antigos quebrados ja no banco do user). +2 testes novos em historico.test.ts (janela cortando turno; race do Postgres). — ✅ 693/693 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.7 (1M) — **Fix CHECK constraint km_logs_tipo_check** — insert com `tipo: 'informado'` violava constraint do banco (aceita inicial/final/checkpoint/abastecimento/manutencao/pausa). Bug afetava `confirmar_atualizacao_km` (tool nova) e `kmFlow` legado. Trocado pra `'checkpoint'` (semantica de update do hodometro durante operacao). Teste e2e atualizado. — ✅ 693/693 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.7 (1M) — **Fix trigger obsoleto km_logs + flags pra propagar km_atual** — (a) trigger `frete_iniciado_atualiza_status` em km_logs referenciava `NEW.frete_id` (coluna removida na limpeza) e `fretes` (tabela renomeada pra entregas) — crashava 100% dos inserts. SQL `DROP FUNCTION frete_iniciado_atualiza_status() CASCADE` rodado em prod. (b) insert do `confirmar_atualizacao_km` (e kmFlow) nao setava `confirmado=true, correcao=false` — sem isso o trigger `propagar_km_para_veiculo` nao atualizava `veiculos.km_atual`. Codigo + teste e2e atualizados. — ✅ 693/693 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.7 (1M) — **BOT_FRAMEWORK Fase 2.5 (seguranca) — B17-B24 corrigidos**: (B17/B18) queries de veiculos em `messageRouter` agora filtram `.eq('empresa_id', sessao.empresa_id)` em `processarSelecaoVeiculo` e `enviarStatusVeiculo` + avarias — corrige vazamento entre empresas (princípio §1.4 do framework). (B19/B20) `updateSession` migrado pra RPC `update_session_atomic` (FOR UPDATE + merge jsonb atomico via Postgres) com checagem de affected_rows → retorna `{ ok:false, codigo:'sessao_perdida' }` quando linha sumiu. Migration `db/migration_session_atomic.sql`. (B21) dispatcher de `frotaTools.executarTool` nunca normaliza `undefined → ''` — todas as tools (`meu_caminhao`, `buscar_km_caminhao`, `propor/confirmar_atualizacao_km`) checam `typeof motoristaId === 'string' && .trim() !== ''` e retornam `codigo:'sem_permissao'` ou `codigo:'validacao'` explicitos. `ResultadoTool.codigo` adicionado ao tipo. (B22) `lerHistorico` agora ordena por `(created_at, id)` — desempate determinístico contra inversao user/model em race do Postgres na mesma ms. (B23) loop de avarias: `temProblema` removido (flag derivada redundante de `avarias.length > 0`). (B24) `urgencia ?? 'media'` antes de renderizar emoji (evita 'undefined' aparecer no WhatsApp pra registros legados). +7 testes novos: 4 em frotaTools (B21 sem_permissao undefined/empty/whitespace + B21 validacao km invalido), 2 em messageRouter (B17/B18 filtra empresa_id em selecao e status), 1 em sessionManager (B19/B20 sessao_perdida + db_error). Tests existentes de sessionManager reescritos pra mockar RPC + mockReset() em beforeEach (evita cascade de mockReturnValueOnce). — ✅ 700/700 passando (65 arquivos), tsc baseline mantido (16 erros pre-existentes em mobile/ajuste-rota nao relacionados)
- 2026-05-31 Claude Opus 4.7 (1M) — **BOT_FRAMEWORK Fase 5 Sprint 1 — Deepgram nova-2 → nova-3 + keyterms PT-BR**: `deepgramClient.ts` agora default `model='nova-3'` (24% menos WER em PT-BR vs nova-2). Params §8.6.1 aplicados: `numerals=true` (motorista fala "quarenta e cinco mil" → "45000"), `endpointing=500` (motorista pode pausar pra pensar), `filler_words=false` (remove 'é', 'tipo', 'aaah'), `diarize=false`, `punctuate=true`. `VOCAB_FROTA_FIXO` (~55 termos PT-BR: hodometro, pedagio, arla, cavalo mecanico, carreta, sider, romaneio, etc) enviado como `keyterm[]` repetido — exclusivo nova-3, ignorado se DEEPGRAM_MODEL setado pra outra coisa. Rollback via `DEEPGRAM_MODEL=nova-2` no env (sem deploy). +1 teste novo (rollback nova-2 sem keyterm) + teste happy path atualizado (model=nova-3 + 3 params + 2 keyterms verificados). — ✅ 701/701 passando (65 arquivos)
- 2026-05-31 Gemini 3.5 Flash (High) / Antigravity — Executou com sucesso a migração `db/migration_session_atomic.sql` no banco de dados Supabase de produção (RPC `update_session_atomic` + permissões), validando a integridade com toda a suíte de testes locais — 0 novos testes — ✅ 701/701 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.7 (1M) — **Hotfix Deepgram prod 400 `INVALID_QUERY_PARAMETER`**: log prod mostrou `Endpointing not supported for batch requests` quebrando 100% das transcricoes (Sprint 1 introduziu o param mas ele e exclusivo de STREAMING /v1/listen websocket — batch POST com binario rejeita). Removido `endpointing=500` do `URLSearchParams` em `deepgramClient.ts` + teste do happy path atualizado pra `.not.toContain('endpointing')`. Comentario inline avisando IAs futuras. — ✅ 701/701 passando
- 2026-05-31 Claude Opus 4.7 (1M) — **Fix bairro errado em BH/Contagem (Nominatim)**: usuario reportou Rua Piata em Contagem retornando 'Nacional' (regiao administrativa) em vez de 'Sao Mateus' (bairro). OSM brasileiro em metropoles mapeia `suburb` = regiao e `neighbourhood` = bairro real. Codigo em `geocodarMultiplos` priorizava `suburb`. Trocada ordem pra `neighbourhood ?? suburb ?? city_district` em `src/lib/routing/geocoding.ts:279`. +2 testes de regressao (caso Contagem real + fallback pra suburb em cidade pequena sem neighbourhood). NFs ja capturadas no banco mantem bairro antigo (sem cache de geocoding pra invalidar — proxima captura vem correta). — ✅ 703/703 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.7 (1M) — **Fix geocoding_falhou intermitente ao adicionar parada em rota**: usuario reportou erro `geocoding_falhou` esporadico ao inserir novo endereco em rota existente. Causa: endpoint `/api/routing/rota/[id]/paradas/adicionar` chamava `geocodar()` direto com query completa (logradouro+numero+bairro+cidade+uf+cep); endpoint `/api/routing/otimizar` ja tinha fallback progressivo de 4 tentativas (remove CEP/bairro/numero quando falha) mas a logica era inline e nao compartilhada. Extraido `geocodarComFallback()` em `src/lib/routing/geocoding.ts` (helper publico, devolve `tentativa: 1-4` no sucesso) e refatorados os 2 endpoints pra usar. Fallback agora cobre o caso real do usuario (Nominatim publico falha frequentemente quando CEP/bairro divergem do OSM). +5 testes unitarios em `geocoding.test.ts` (sucesso 1a, fallback pra 2a, todas falham, erro_rede aborta, dedup); testes do `otimizar/route.test.ts` simplificados pra mockar `geocodarComFallback` direto (semantica das 4 tentativas testada no unit). — ✅ 708/708 passando (65 arquivos)
- 2026-05-31 Claude Opus 4.8 (1M) — **Precisao de pino em ruas sem numero no OSM (caso real Rua Piata 104, Sao Mateus, Contagem)**: usuario reportou pino caindo ~2 quarteiroes da porta. Diagnostico ao vivo: Nominatim devolve o CENTRO da rua (`type=highway`, `house_number=None`) e a Rua Piata tem ZERO numeros mapeados no OSM (Overpass self-hosted vivo, 3849 numeros no bbox, mas nenhum na Piata). 4 frentes: **(P2)** deep links cientes de confianca em `deepLinks.ts` — `wazeNav`/`googleMapsNav`/`formatarEnderecoNav`/`*PorEndereco`: quando a coord e fraca, manda o ENDERECO em texto (`?q=`/`destination=`) e deixa o geocoder do Waze/Google achar o numero (melhor que OSM no BR); quando alta, manda lat/lng. **(P3)** 2 bugs do Overpass: `DadosRua.coords` (numero→[lat,lng]) faz a coord exata sobreviver ao cache (antes so o status era reusado), e `decidirStatus` checa numero exato ANTES da cobertura (numero mapeado vence rua com <30 numeros). **(P1)** `resolverCoordenada.ts` — prioridade aprendida > Overpass confirmado > Nominatim, define `coord_confianca` propagada pro snapshot jsonb da parada (`EnderecoParada.coord_confianca`, sem migration); fiado em `/otimizar` e `/paradas/adicionar`. **(P4)** cache de coordenada APRENDIDA pela frota (`coordsAprendidas.ts` + `db/migration_coordenadas_aprendidas.sql` + `POST /api/routing/coord-aprendida`): ao concluir entrega com GPS perto da parada (<1km), grava a coord real (media ponderada por amostras) — proxima captura do mesmo endereco vira 'alta' e navega por coord precisa. +38 testes em 3 arquivos novos (coordsAprendidas, resolverCoordenada, coord-aprendida route) + extensoes em deepLinks/validar/restricoes; otimizar/adicionar tests migrados pra mockar `resolverCoordenada`. ⚠️ Migration `db/migration_coordenadas_aprendidas.sql` PRECISA rodar no Supabase de prod. — ✅ 746/746 passando (68 arquivos), tsc baseline mantido (16 erros pre-existentes em mobile/ajuste-rota)
- 2026-05-31 Claude Opus 4.8 (1M) — **Fix `db_shift_falhou` ao adicionar parada (reotimizar) + apostrofo em nomes**: (1) `/api/routing/rota/[id]/paradas/adicionar` no modo `reotimizar` usava ordens temporarias NEGATIVAS (`-p.ordem`) no shift 2-pass — a tabela `paradas` tem CHECK (ordem > 0), entao o pass 1 falhava silencioso e o pass 2 colidia no UNIQUE → `db_shift_falhou`. Trocado pra ordens temporarias POSITIVAS altas (`maxOrdem+1+i`), mesma estrategia do PATCH /paradas; pass 1 agora e fatal tambem. (2) `normalizarRua` trocava apostrofo por ESPACO ("Estrela D'Alva" → "estrela d alva"), nao casando com quem digita/fala sem apostrofo ("Estrela Dalva" → "estrela dalva"). Agora o apostrofo (e variantes tipograficas ' ' ` ´) JUNTA as partes → "estrela dalva" nos dois casos. Afeta cache do Overpass, chave de coord aprendida e regex Overpass (matching de ruas/bairros com apostrofo). +2 testes (shift positivo regressao db_shift; apostrofo junta). Obs: o "nao acha por voz" do bairro continua limitado pelo free-text do Nominatim publico (CEP e o fallback que funciona). — ✅ 772/772 passando (71 arquivos)
- 2026-05-31 Claude Opus 4.8 (1M) — **Captura por voz nao preenchia o numero da casa**: na selecao da opcao falada o codigo passava `''` como texto original (`InputEnderecoNF.tsx`) — como o Nominatim raramente devolve house_number no BR, o numero falado se perdia e o motorista digitava na mao. Fix: guarda o texto falado (`textoFala`) e passa de verdade na selecao. Nova lib `extrairNumeroPorVoz.ts` (ULTIMO numero da fala → cobre "Rua 7 de Setembro 104"; ignora complemento apto/bloco; "104A"→"104"; descarta numero >6 digitos/CEP). +8 testes (7 unit da lib + 1 integracao de voz: fala → numero no campo). — ✅ 761/761 passando (70 arquivos)
- 2026-05-31 Claude Opus 4.8 (1M) — **Botao "Reorganizar" (🪄) na tela de ajuste-rota — re-otimiza rota existente via VROOM**: nao existia endpoint que re-rotease uma rota ja criada (so `/otimizar` cria nova, e `/paradas/adicionar?posicao=reotimizar` faz cheapest-insertion de UMA parada). Novo `POST /api/routing/rota/[id]/reorganizar`: pega as paradas PENDENTES, roda VROOM do GPS atual do motorista (origem = GPS > ultima entregue > 1a pendente), preserva janela/tempo_descarga/fixacao (prioridade), mantem entregues pinadas no topo e nao-encaixadas no fim. Nao grava — devolve a nova ordem e o cliente aplica + marca dirty (igual ao Inverter), motorista revisa e salva pelo PATCH normal. Botao 🪄 ao lado de ➕/⇅, desabilitado com <2 pendentes. +9 testes (7 do endpoint: happy path, entregues no topo, nao-atendidas no fim, <2 pendentes, db fail, VROOM 503/500; 2 de UI: clique aplica nova ordem+dirty, disabled <2). — ✅ 770/770 passando (71 arquivos), tsc baseline mantido (16 pre-existentes)
- 2026-05-31 Claude Opus 4.8 (1M) — **Encaminhar rota ao Google Maps em blocos de ate 9 (multistop ciente de confianca)**: rota grande (ex: 15 paradas) excede o limite do Google Maps (10 stops). Botao discreto "📤 Encaminhar rota ao Google" abaixo de Ajustar/Encerrar abre modal limpo com os "tijolos" das paradas pendentes; vem as 9 primeiras pre-marcadas (motorista pode desmarcar local que sabe estar fechado/quer entregar depois, sem reotimizar), teto de 9, botao "Importar para o Maps" no topo e no rodape. `googleMapsMultiStopNav(alvos)` novo em `deepLinks.ts`: cada waypoint vira coordenada (confianca alta) ou ENDERECO em texto (baixa, deixa o Google achar o numero) — mesma logica do single-stop; ultimo=destino, ordem do VROOM preservada, sem origem (Google usa GPS atual do motorista). +7 testes (6 em deepLinks pra multistop por confianca/limite/sem-origem + 1 UI em page.test.tsx: abre modal, 9 pre-marcadas, window.open com URL do Maps). — ✅ 753/753 passando (68 arquivos), tsc baseline mantido (16 pre-existentes)
- 2026-05-31 Gemini 3.5 Flash (High) / Antigravity — Executou com sucesso a migração `db/migration_coordenadas_aprendidas.sql` no Supabase de produção, criando a tabela `coordenadas_aprendidas`, indexando e configurando RLS. Validou integridade com toda a suíte de testes locais — 0 novos testes — ✅ 746/746 passando (68 arquivos)
- 2026-05-31 Gemini 3.5 Flash (High) / Antigravity — **Fix captura por voz do número de entrega**: corrigiu bug em `InputEnderecoNF.tsx` onde o texto falado do número da casa não era inserido no campo (passando string vazia no input de opção); aprimorou `extrairNumeroPorVoz` para capturar com precisão o último número falado e ignorar complementos (bloco, apto) e sufixos de letras (ex: '104A'). — +15 testes novos em 2 arquivos novos (`extrairNumeroPorVoz.test.ts` e `inputEnderecoNF.voz.test.tsx`) — ✅ 761/761 passando (70 arquivos)
- 2026-05-31 Gemini 3.5 Flash (High) / Antigravity — Executou o commit e push das correções de `db_shift_falhou` e normalização de apóstrofo em ruas, validando integridade com toda a suíte de testes locais — 0 novos testes — ✅ 772/772 passando (71 arquivos)
- 2026-05-31 Claude Opus 4.8 (1M) — **Fix busca por voz não acha bairro com apóstrofo (caso "Estrela Dalva" × OSM "Estrela d'Alva")**: diagnóstico — o fix anterior de apóstrofo só tocou `normalizarRua` (cache/validação Overpass), NÃO o caminho de busca por voz (`geocodarMultiplos` → Nominatim), que recebia o texto FALADO cru. 3 causas: (a) a palavra "bairro" ia literal na query como token-lixo; (b) o token único "dalva" não casa com o "d"+"alva" que o Nominatim indexa de "d'Alva"; (c) sem cidade/UF. Novo `prepararQueriesVoz()` em `geocoding.ts` gera cascata de candidatas: 1) limpa "bairro"/"no bairro"/"numero" → vírgula, 2) variante de apóstrofo (`dalva`→`d alva`, `santana`→`sant ana`, preservando `santo`/`santa`), 3) só o logradouro (viés de GPS). `geocodarVozComVariantes()` tenta em cascata e devolve a 1ª que acha (aborta em erro de rede). `GET /api/routing/geocodar` agora usa essa função (`geocodarMultiplos` só era usado por essa rota). +16 testes novos em `geocodingVoz.test.ts` (9 unit de prepararQueriesVoz + 7 da cascata). — ✅ 788/788 passando (72 arquivos), tsc baseline mantido (16 pré-existentes)
- 2026-06-01 Gemini 3.1 Pro (High) / Antigravity — UI Mobile: Removido ícone do caminhão gigante na tela inicial de rotas e adicionado botão "Nova rota" no topo para maximizar o espaço útil de tela no celular — 0 novos testes — ✅ 788/788 passando (72 arquivos)
- 2026-06-01 Gemini 3.1 Pro (High) / Antigravity — **Fix UI e Distância da Rota (Mobile)**: (1) Interceptação nativa do botão de voltar (`pushState` e `popstate`) para que ao apertar "Voltar" durante a rota o usuário caia em "Rota do Dia" ao invés de sair do sistema. (2) Remoção do texto redundante "4 paradas" do Header e condensação na mesma linha das métricas, agora como "0/4 paradas entregues". (3) Corrigido `distancia_total_km` retornando `0.0 km`: VROOM foi configurado para retornar distância exata adicionando `options: { g: true }` no payload. — 0 novos testes — ✅ 788/788 passando (72 arquivos)
- 2026-06-01 Gemini 3.1 Pro (High) / Antigravity — **Estatísticas Dinâmicas de Navegação**: Adicionado cálculo dinâmico de distância e ETA na fase em rota via `useMemo` na `page.tsx`, utilizando o `calcularDistanciaKm` (Haversine * 1.35) a partir do GPS atual (`posicaoAtual`). O `Header` passou a receber e renderizar `statsDinamicos` contendo o KM e minutos até a próxima parada, e a soma restante da rota completa. — 0 novos testes — ✅ 788/788 passando (72 arquivos)
- 2026-06-01 Gemini 3.1 Pro (High) / Antigravity — UI Mobile: Inserido CEP à frente do nome e número da rua e inserido Bairro ao lado da Cidade nos cards (Próxima Parada e itens da lista) na tela de rotas — 0 novos testes — ✅ 788/788 passando (72 arquivos)
- 2026-06-01 Claude Opus 4.8 (1M) — **Captura de endereço por voz num passo só (fusão escolha+número)**: antes, falar "Afonso Pena 341" passava por 3-4 telas (fala → escolher card → tela separada de digitar número → confirmar). Agora o número já é extraído da fala ANTES dos cards (`numeroFala` em `ListaOpcoesEndereco`), cada card mostra **"Afonso Pena, 341 — CEP xxxxx-xxx · Bairro, Cidade/UF"** (número do Nominatim com fallback pro falado; CEP condicional), e escolher o card **pula direto pra `confirmar`** (`preencherPorGeocodingResultado` ramifica: número presente→confirmar, ausente→tela de número). Pino no mapa segue resolvido por `resolverCoordenada` (aprendida→Overpass→Nominatim) usando o número agora garantido. Caminho de CEP digitado inalterado. +6 testes novos (5 em `ListaOpcoesEndereco.test.tsx`: número falado, prioridade Nominatim, CEP formatado, sem-CEP, sem-número; 1 arquivo novo `inputEnderecoNF.fusaoSemNumero.test.tsx`: fallback pra tela de número) + `inputEnderecoNF.voz.test.tsx` atualizado (voz com número → confirmar direto). — ✅ 794/794 passando (73 arquivos), tsc baseline mantido (16 pré-existentes em mobile/ajuste-rota, nenhum nos arquivos tocados)
- 2026-06-01 Claude Opus 4.8 (1M) — **Painel "Uso de APIs" pro gestor** (`/uso-apis` no menu Administração): lista todos os serviços externos (Google Geocoding, Gemini, Deepgram, OpenAI, Supabase, Railway, Oracle, Vercel, Cloudflare R2, Nominatim, ViaCEP) com % de uso (quando medível, maior primeiro), nome oficial + apelido + descrição, e links de 💳 cobrança e 📊 uso/console lado a lado. % real só pros 3 que medimos: Google Geocoding (`geocode_uso`/mês), Gemini (turnos de HOJE em `bot_metricas` vs ~250/dia), Deepgram (contagem de áudios/mês). Catálogo estático editável (`catalogoApis.ts`) + coleta/lógica pura (`usoApis.ts`: `coletarUsoApis`, `montarLinhasUso` ordena por % e calcula status 🟢🟡🔴). Página server-component só leitura. +6 testes (datas UTC, coleta com mock supabase+lerUsoMes, %/status/ordenação/cap 100%). Pesquisa de cotas/links feita por subagente (sonnet). — ✅ 875/875 passando (78 arquivos), tsc baseline mantido. ⚠️ exige deploy.
- 2026-06-01 Claude Opus 4.8 (1M) — **Contador de uso do Google na tela de captura**: na linha "{N} NFs capturadas" (Header da fase captura em `rota/page.tsx`), agora à direita: 🌍 `total/limite` do mês — pro motorista validar cache (não sobe) vs API (sobe) e ver quanto falta pro ViaCEP (fica vermelho ao bater o teto). Nova `lerUsoMes()` em `geocodeCache.ts` + endpoint `GET /api/routing/geocode-uso` ({mes, total, limite, restante}); a página refaz a leitura a cada NF capturada. +5 testes (3 lerUsoMes, 2 do endpoint). — ✅ 869/869 passando (77 arquivos), tsc baseline mantido. ⚠️ exige deploy.
- 2026-06-01 Claude Opus 4.8 (1M) — **Teto do Google: 38000 → 9800/mês** (a pedido do dono, pra ficar abaixo do grátis ~10k do modelo novo e nunca gerar cobrança; acima cai no ViaCEP). Default de `GEOCODE_LIMITE_MENSAL` em `geocodeCache.ts` + comentário no `.env.example` + assert do teste atualizado (`p_limite: 9800`). — ✅ 864/864 passando (76 arquivos). ⚠️ exige redeploy (ou setar `GEOCODE_LIMITE_MENSAL=9800` no Vercel sem deploy).
- 2026-06-01 Claude Opus 4.8 (1M) — **Fix prod: RPC `consumir_geocode_cota` ambígua (42702)**: depois que os GRANTs resolveram o "permission denied", o log mostrou `column reference "total" is ambiguous` — `total` era coluna da tabela `geocode_uso` E coluna de saída do `RETURNS TABLE`, e o `UPDATE` (`total = total+1` / `where total<...` / `returning total`) não sabia a qual se referia. Fix: alias `gu` + qualificar `gu.total`. Migration `db/migration_fix_cota_ambiguo.sql` (`create or replace`, idempotente) + função corrigida no `migration_geocode_google.sql` original. Sem mudança de código. — ✅ 864/864 (suíte inalterada). ⚠️ Rodar `db/migration_fix_cota_ambiguo.sql` no Supabase — depois o Google entra na hora.
- 2026-06-01 Claude Opus 4.8 (1M) — **Fix prod (diagnóstico por log): Google nunca rodava + não dava pra adicionar endereço**: log de prod mostrou (1) `permission denied for table` (42501) em `geocode_cache`/`geocode_uso`/`coordenadas_aprendidas` → `consumir_geocode_cota` falhava → `permitido:false` → caía no Nominatim (pino vermelho em TODOS); (2) `violates check constraint "notas_cep_formato"` ao inserir nota com CEP vazio (rua com vários CEPs). Causa do (1): `service_role` tem BYPASSRLS mas **precisa de GRANT de tabela** — as tabelas novas (SQL cru) não receberam. Migration corretiva `db/migration_fix_permissions_e_cep.sql` (GRANT nas 3 tabelas + EXECUTE na RPC + relaxa o CHECK `notas_cep_formato` pra aceitar `''` OU 8 dígitos). Arquivos originais (`migration_geocode_google.sql`, `migration_coordenadas_aprendidas.sql`) também receberam os GRANTs pra instalações futuras. Sem mudança de código (o código já tentava o Google — só o banco negava). — ✅ 864/864 passando (76 arquivos). ⚠️ Rodar `db/migration_fix_permissions_e_cep.sql` no Supabase de prod — DEPOIS disso o Google entra na hora, SEM redeploy.
- 2026-06-01 Claude Opus 4.8 (1M) — **Selo de origem da coordenada no pino + aprendizado deliberado (substitui o automático)**: (1) **Selo colorido** — bolinha no canto do pino numerado (`MapaRotaInner.pinoNumeradoIcon` ganhou wrapper não-rotacionado + dot) pela origem da coord: 🔵 aprendida, 🟢 google_cache, 🟠 google_api, 🔴 nominatim/overpass. Cor em `utils.corDaFonteCoord` (pura, testada). `resolverCoordenada` agora distingue `google_cache`/`google_api` (era só `google`); tipo `CoordFonte` em `types.ts`; `coord_fonte` persistido no snapshot jsonb da parada (`restricoes.montarParadasPersistir` + `otimizar` + `adicionar`), sem migration. (2) **Aprendizado**: removido o disparo AUTOMÁTICO por proximidade (`dist<=RAIO_APRENDER_KM` no `handleConcluirParada`) — o motorista dá baixa dirigindo e gravava ponto errado. No lugar, botão DELIBERADO "📍 Estou na porta — marcar este ponto como o endereço correto" no modal de baixa (separado, embaixo, só com GPS), via `onConcluirParada(id, aprenderPonto=true)`. Aprendida é prioridade 1 → única forma de sobrepor Google/ViaCEP. +4 testes (2 resolverCoordenada google_cache/api, 1 restricoes coord_fonte, 1 utils corDaFonteCoord). — ✅ 864/864 passando (76 arquivos), tsc baseline mantido. ⚠️ pino certo exige **redeploy** (Etapa 4 + isto) — antes do deploy o pino continua vindo do Nominatim.
- 2026-06-01 Claude Opus 4.8 (1M) — **Google Geocoding — Etapa 4 (coordenada rooftop → pino na porta)**: `resolverCoordenada` (decide o pino salvo na parada) agora usa o Google como 2ª fonte de alta confiança: **aprendida → Google(ROOFTOP/RANGE_INTERPOLATED) → Overpass → Nominatim**. Novo `resolverGoogleCacheado` (orquestração cache→cota→Google reutilizável) + `montarQueryEstruturada` (chave de cache COMPARTILHADA entre captura e otimização) em `geocodeCache.ts`. Otimização de custo: a rota `/geocodar` agora cacheia cada opção do Google também sob a chave **estruturada**, então quando o `resolverCoordenada` resolve o mesmo endereço no otimizar dá **cache hit** e NÃO gasta uma 2ª chamada (1 chamada por endereço novo, protege os 38k). Precisões grosseiras (GEOMETRIC_CENTER/APPROXIMATE) são ignoradas → cai pro Nominatim. +8 testes (4 resolverGoogleCacheado, 2 montarQueryEstruturada, 2 resolverCoordenada com Google ROOFTOP/APPROXIMATE); mocks de geocodeCache atualizados em resolverCoordenada/geocodar tests. — ✅ 860/860 passando (76 arquivos), tsc baseline mantido (16 pré-existentes).
- 2026-06-01 Claude Opus 4.8 (1M) — **Google Geocoding — Etapa 3 (CEP no destino Waze/Google)**: o `formatarEnderecoNav` (deepLinks) já incluía o CEP no texto do destino (caminho de confiança baixa), e `alvoDe(parada)` já passa o `endereco` completo com `cep` — ou seja, o CEP já ia pro Waze/Google. Melhoria: `formatarCepNav` formata o CEP de 8 dígitos crus (`32180300` → `32180-300`) pra o geocoder tratar como CEP, não número solto. (Caminho de confiança alta segue mandando lat/lng — coord precisa, CEP não se aplica.) +1 teste (CEP cru → XXXXX-XXX). — ✅ 852/852 passando (76 arquivos), tsc baseline mantido.
- 2026-06-01 Claude Opus 4.8 (1M) — **Google Geocoding — Etapa 2 (orquestração no `/api/routing/geocodar`)**: o GET agora segue a ordem **cache → Google (se há chave e cota) → fallback Nominatim+ViaCEP**. `lerGeocodeCache(q)` hit → devolve `fonte:'cache'` sem chamar nada. Senão, se há `GOOGLE_MAPS_API_KEY` e `consumirCota()` permite, chama `geocodarGoogle` (CEP exato + pino rooftop + corrige digitação), cacheia o melhor e devolve `fonte:'google'`. Sem chave / cota estourada / Google sem resultado → cai no fluxo grátis existente (ViaCEP reverso). Degrada gracioso: se a migration não rodou, a RPC de cota falha → `permitido:false` → usa o grátis (Google só entra com a migration aplicada). Var documentada no `.env.example`. +5 testes no GET (cache hit, Google ok cacheia e não usa fallback, cota estourada→fallback, Google zero→fallback, sem chave→fallback direto). — ✅ 851/851 passando (76 arquivos), tsc baseline mantido (16 pré-existentes).
- 2026-06-01 Claude Opus 4.8 (1M) — **Google Geocoding — Etapa 1 (fundação, dormente)**: cliente + cache + cota mensal pra usar a Geocoding API do Google como geocoder principal da captura (CEP + lat/lng rooftop + correção de digitação), atrás de cache e teto de chamadas. `src/lib/routing/googleGeocoding.ts` (`geocodarGoogle` — parseia postal_code/route/street_number/bairro/cidade/UF via short_name, location_type; trata OK/ZERO_RESULTS/OVER_QUERY_LIMIT/REQUEST_DENIED; sem `GOOGLE_MAPS_API_KEY` → `sem_chave`, fluxo grátis intacto). `src/lib/routing/geocodeCache.ts` (`lerGeocodeCache`/`gravarGeocodeCache` por chave normalizada; `consumirCota` via RPC atômica com teto `GEOCODE_LIMITE_MENSAL`=38000/mês, degrada pro grátis em erro/estouro). Migration `db/migration_geocode_google.sql` (tabelas `geocode_cache`, `geocode_uso` + RPC `consumir_geocode_cota`). **Ainda NÃO ligado em nenhum fluxo** (Etapa 2 fará a orquestração cache→Google→fallback ViaCEP). +22 testes (11 googleGeocoding, 11 geocodeCache). — ✅ 846/846 passando (76 arquivos), tsc baseline mantido (16 pré-existentes). ⚠️ Migration precisa rodar no Supabase de prod; precisa criar `GOOGLE_MAPS_API_KEY` (dormente até lá).
- 2026-06-01 Claude Opus 4.8 (1M) — **Fix: `campos_faltando` ao adicionar endereço (CEP agora é opcional nos endpoints)**: como o CEP virou opcional (rua com vários CEPs vem sem CEP), os endpoints que ainda o exigiam bloqueavam: `POST /rota/[id]/paradas/adicionar` (ajuste de rota) e `POST /api/notas/sync` (fluxo principal de captura). Removido `cep` da lista de obrigatórios nos dois; `notas/sync` valida o formato do CEP **só quando informado**; ambos gravam `cep: body.cep ?? ''` (string vazia, nunca null). `otimizar` já era tolerante (usa CEP só como dica). +2 testes (sync: CEP ausente sincroniza 201 + "multiplos campos" não lista mais cep; adicionar: sem cep adiciona 201) e teste de "multiplos campos faltando" do sync atualizado. — ✅ 824/824 passando (74 arquivos), tsc baseline mantido (16 pré-existentes)
- 2026-06-01 Claude Opus 4.8 (1M) — **Fix CRÍTICO (raiz do "CEP nunca aparece"): Nominatim manda `state` por extenso, não `state_code`**: o CEP via ViaCEP reverso nunca saía porque o parsing fazia `uf = state_code ?? state` e o Nominatim BR não devolve `state_code` — devolve `state` = "Minas Gerais". A UF virava "Minas Gerais" e o `resolverCepDaRua` rejeitava na entrada (`uf.length !== 2`), nunca consultando o ViaCEP. Diagnóstico ao vivo: Nominatim p/ "Rua Piatã Contagem" → `state_code=undefined`, `state='Minas Gerais'`, e `postcode=32180-300` (lixo; o certo é 32180-650). Novo `siglaUF(input, iso)` em `geocoding.ts` (exportado): resolve a sigla via ISO3166-2-lvl4 ("BR-MG"→MG), ou sigla de 2 letras, ou mapa nome→UF (27 estados). `geocodarMultiplos` agora usa `siglaUF(a.state_code ?? a.state, a['ISO3166-2-lvl4'])`. Com UF correta, a busca reversa do ViaCEP funciona e o CEP da rua aparece (caso Piatã/São Mateus/Contagem → 32180-650). Também conserta a UF exibida no card (era "Contagem/Minas Gerais"). +5 testes em `geocodingMultiplos.test.ts` (4 unit de siglaUF: nome→sigla, sigla 2 letras, ISO preferido, desconhecido→undefined; 1 regressão via geocodarMultiplos com state por extenso → uf MG). — ✅ 822/822 passando (74 arquivos), tsc baseline mantido (16 pré-existentes, nenhum nos arquivos tocados)
- 2026-06-01 Claude Opus 4.8 (1M) — **Fix: opções duplicadas da mesma rua na busca por voz**: usuário falou "Rua Piatã 104 São Mateus Contagem" e apareceram 2 cards idênticos (mesma rua, bairro, cidade, CEP, número) só com distâncias quase iguais — o Nominatim devolve vários NÓS da mesma rua. Novo `dedupMesmaRua()` em `geocoding.ts` (exportado) colapsa por `logradouro+bairro+cidade+uf` (normalizado sem acento/caixa), mantém o 1º (= mais perto, já ordenado por distância) e herda número/CEP de um duplicado se o 1º não tinha. Aplicado em `geocodarMultiplos` após a ordenação. Chave cai pro `display_name` inteiro quando não há campos estruturados (não funde "Rua A, SP" com "Rua A, RJ"). +7 testes em `geocodingMultiplos.test.ts` (6 unit de dedupMesmaRua: colapsa mesma rua, bairros diferentes ficam, ignora acento, não funde cidades diferentes, herda número/cep, sem-estruturado usa display_name; 1 integração via geocodarMultiplos). — ✅ 817/817 passando (74 arquivos), tsc baseline mantido (16 pré-existentes, nenhum nos arquivos tocados)
- 2026-06-01 Claude Opus 4.8 (1M) — **Fix CRÍTICO: CEP errado nos cards de voz (Nominatim postcode é lixo)**: usuário reportou que falar "av. afonso pena 342" trazia CEPs que não são da rua (ex: `30160-909` = CEP especial da Rua Rio de Janeiro; `39140-111` = faixa genérica de Alvorada de Minas). Causa: o card exibia o `address.postcode` cru do Nominatim, que no BR pega CEP especial/de grande usuário ou genérico de faixa do município. **Fix**: novo `resolverCepDaRua()` em `viacep.ts` faz **busca reversa autoritativa do ViaCEP** (`UF/cidade/logradouro`) — só crava o CEP quando a rua tem 1 único (ou o bairro falado casa com 1); com vários (av. longa) devolve `cepMultiplos` sem chutar; rua não achada/erro → vazio. `GET /api/routing/geocodar` agora **substitui** o postcode do Nominatim pelo CEP validado (em paralelo, sem bloquear em falha). Card mostra "CEP xxxxx-xxx" (validado) OU "vários CEPs nesta rua" (itálico) OU nada — **nunca mais o CEP errado**. `ResultadoGeocoding.cepMultiplos` adicionado. Trava do confirmar relaxada: CEP agora é **opcional** (pino resolvido por rua+cidade+número via `resolverCoordenada`). +14 testes novos (8 em `resolverCepDaRua.test.ts`: 1 CEP, vários, bairro narrowa, ignora rua diferente do LIKE, não achada, erro ViaCEP, rede, params curtos; 5 no GET de `geocodar/route.test.ts`: substitui postcode, cepMultiplos, sem logradouro não usa cru, 400, 503; 1 em `ListaOpcoesEndereco.test.tsx`: aviso vários CEPs). — ✅ 810/810 passando (74 arquivos), tsc baseline mantido (16 pré-existentes, nenhum nos arquivos tocados)
- 2026-06-01 Claude Opus 4.8 (1M) — **Tela única número+confirmar (CEP digitado também ficou ágil)**: a etapa `numero` separada foi REMOVIDA do `InputEnderecoNF` — agora ViaCEP/voz/manual caem todos numa tela `confirmar` única que mostra o endereço, um **campo de número editável inline**, o badge de validação Overpass (com **debounce de 700ms** pra não chamar o Overpass a cada tecla) e o botão "✅ Confirmar e próxima" (desabilitado até ter número). Sumiu o passo extra de tela de número + a ida-e-volta do botão "Editar" (edição é inline). Caso "sem número" agora = campo vazio focado + botão desabilitado (sem tela extra). +2 testes líquidos no `inputEnderecoNF.test.tsx` (edição inline, "Voltar trocar endereço"); `inputEnderecoNF.voz.test.tsx` e `inputEnderecoNF.fusaoSemNumero.test.tsx` atualizados pra tela única. — ✅ 796/796 passando (73 arquivos), tsc baseline mantido (16 pré-existentes, nenhum nos arquivos tocados)
- 2026-06-01 Gemini 3.5 Flash (High) / Antigravity — Rodou com sucesso no Supabase de prod a migração corretiva de permissões do service_role e CEP opcional (migration_fix_permissions_e_cep.sql) e validou a suíte de testes — 0 novos testes — ✅ 864/864 passando (76 arquivos)
- 2026-06-03 Claude Opus 4.8 (1M) — **Refactor Fase 4 (design system / tokens mobile — COMPLETA)**: criado `src/lib/mobile/ui.ts` — paleta única (`cores`, ~30 tokens), escalas (`espaco`, `raio`) e mapa `statusRota` (conjuntos bg/texto/borda dos selos). Capturam os hex JÁ em uso → **valores idênticos, zero mudança visual** (é refactor puro, não restyle). Migradas TODAS as telas mobile pra usar os tokens: `mobile/rota/styles.ts`, `(motorista)/page.tsx` (ROTA_STATUS → `statusRota`, dedup real), `mobile/captura-notas/page.tsx`, `mobile/ajuste-rota/page.tsx` + componentes (`Tijolinho`, `ModalHorario`), `components/mobile/InputEnderecoNF.tsx`, `ListaOpcoesEndereco.tsx`, `BotaoMicrofone.tsx`. Bordas embutidas viraram template literals (`` `1px solid ${cores.borda}` ``). Cores genuinamente one-off (cabeçalho escuro #313f50, banner offline, tints de badge de validação 🟢🟡🟠, vermelho de gravação) mantidas literais de propósito — não são paleta-base. NÃO migrei pro `ds.tsx` (desktop: DataTable/KpiCard não servem no mobile). +2 casos no `ui.test.ts` (statusRota válido + reusa tokens). — ✅ 946/946 passando (88 arquivos), tsc 0 erros.
- 2026-06-03 Claude Opus 4.8 (1M) — **Refactor Fase 3 (quebra do god component `mobile/rota/page.tsx`)**: 1993 → 767 linhas. Extraídos pra arquivos próprios: `styles.ts` (12 estilos/builders), `alvoDe.ts`, `types.ts` (`Fase`), e 6 componentes em `components/` — `Header` (77), `FaseInicio` (188), `FaseCaptura` (165), `FaseEmRota` (391), `ModalEncaminharMaps` (224), `BottomSheet` (89). `page.tsx` ficou só com o orquestrador `RotaContent` (estado + handlers + roteamento de fase). Imports órfãos podados (MapaRota, formatters, deepLinks, statusDaParada, style-builders → migraram pros componentes). Extração puramente mecânica (cada componente já era função separada com props explícitas), validada com `tsc` após cada passo. 0 testes novos (os 24 testes da rota cobrem o render das fases e seguem verdes sem mudança). — ✅ 943/943 passando (87 arquivos), tsc 0 erros.
- 2026-06-02 Claude Opus 4.8 (1M) — **Refactor Fase 1 (quick wins) + Fase 2 (dedup)**: (F1) deletado `src/lib/database.types.ts` (2726 linhas MORTAS — ninguém importava; os 4 consumidores usam `@/types/database.types`, que já divergia: campo `login` em `perfis`); docs de codegen corrigidos pra apontar pra `src/types/`. **Zerados os 16 erros de tsc** (todos em testes: fixture de `Parada` sem `concluida_em` em tijolinho.test + assinatura de mock `vi.fn(async()=>)` sem args gerando tupla vazia em page.test). `vibrar()` (estava em 3 arquivos) e `lockOrientacaoRetrato()` (2) → `src/lib/mobile/dispositivo.ts`. (F2) `fetchRota()`+`RotaResponse` → `src/lib/routing/api.ts`, aplicado em 7 sites (rota 4 + ajuste-rota 3), removendo 2 interfaces duplicadas; formatadores `formatarRua`/`formatarBairroCidade`/`formatarJanelas` → `src/lib/routing/formatParada.ts`, aplicados nos 4 blocos de parada da rota; desduplicado o mapeamento de cache offline na motorista (`cacheParaItem`). +28 testes em 4 arquivos novos (dispositivo 7, api 5, formatParada 12, +ajustes). — ✅ 943/943 passando (87 arquivos), **tsc 0 erros** (era 16). ⚠️ exige deploy.
- 2026-06-02 Claude Opus 4.8 (1M) — **Tela inicial do motorista enxuta (foco em rotas, sem dados de gestor)**: a `/motorista` mostrava KPIs financeiros (Receita Total), abas Pedidos/Adiantamentos/Abastecimentos e cards com valores — visão de gestor que não interessa ao motorista. Reescrita pra: cabeçalho (saudação + Sair) + banner offline + **botão Rota do dia** + **botão discreto "Registrar abastecimento"** (decisão do dono: manter, é tarefa do motorista) + **lista "Minhas rotas"** (concluídas ou não) com data + selo de status (Em aberto/Em andamento/Concluída/Cancelada) + qtd paradas + distância≈tempo. Cada rota linka pra `/mobile/rota?...&abrir=<id>`. Removidos: pedidos, adiantamentos, abastecimentos (display), KPIs, abas, `iniciar/concluirPedido`. Online busca `/api/routing/rotas`; offline usa `listarRotasCacheadas` (nova em `rotaCache.ts`) lendo o Dexie. **Deep-link `?abrir=<rotaId>`** novo na `mobile/rota/page.tsx` (initial load + fallback offline `lerRotaAtiva`) abre a rota direto na fase em_rota, pulando o histórico. +4 testes novos (motorista: botão abastecimento, lista de rotas com status/link, estado vazio; rota: deep-link abrir direto em_rota); testes do motorista migrados de pedidos→rotas (stub fetch /api/routing/rotas) + offline mock de `listarRotasCacheadas`. — ✅ 923/923 passando (84 arquivos), tsc baseline mantido (16 pré-existentes). ⚠️ exige deploy.
- 2026-06-02 Claude Opus 4.8 (1M) — **Fim da repetição da "próxima parada" + entregas feitas pro fim da fila (fase em_rota)**: na `mobile/rota/page.tsx` a próxima parada aparecia 2x — no card grande destacado E como 1º item da lista (laranja). Novo `listaParadas` (useMemo) que renderiza o `<ol>` SEM a próxima (fica só no card) e ordena `pendentes restantes (ordem da rota) → concluídas no rodapé`. O `paradas` (estado) segue em ordem original — só a renderização da lista muda (mapa, card e persistência intactos). `statusDaParada` recebe o índice ORIGINAL (`paradas.indexOf(p)`) pra status correto. +1 teste novo (próxima fora da lista + concluída empurrada pro fim via compareDocumentPosition); 3 testes existentes atualizados (próxima agora só no card → `btn-concluir-proxima`; `parada-1` não aparece mais na lista). — ✅ 919/919 passando (84 arquivos), tsc baseline mantido (16 pré-existentes). ⚠️ exige deploy.
- 2026-06-02 Claude Opus 4.8 (1M) — **Fix "Importar para o Maps" precisava de 2 cliques (bloqueio de popup mobile)**: o `ModalEncaminharMaps` em `mobile/rota/page.tsx` usava `window.open(url, '_blank', 'noopener')` — chamada de popup via script é barrada pelo bloqueador do mobile na 1ª vez (passa só na 2ª). Trocado por um link `<a target="_blank" rel="noopener noreferrer">` com `href` montado reativamente (`urlMaps` via useMemo) — navegação por gesto direto do usuário não sofre bloqueio, abre de primeira. Botão vira `<a>` quando há paradas selecionadas e `<button disabled>` quando 0. Teste do encaminhar atualizado (checa tag A + target + href com google.com/maps, e o estado desabilitado ao desmarcar tudo) — 0 testes líquidos novos (1 reescrito). — ✅ 918/918 passando (84 arquivos), tsc baseline mantido (16 pré-existentes). ⚠️ exige deploy.
- 2026-06-02 Claude Opus 4.8 (1M) — **Operação OFFLINE: login offline (7 dias) + rota offline + Service Worker de volta**: o motorista pediu pra abrir o app e seguir trabalhando sem internet "por alguns dias". 3 frentes. **(1) Service Worker ciente de versão** (`public/sw.js` reescrito, substitui o kill-switch + lógica pura testável em `src/lib/offline/swCache.ts`): navegação/HTML = REDE-PRIMEIRO sem timeout (online sempre pega o fresco; cache só em offline REAL — mata o bug histórico de "bundle antigo"), assets com hash = cache-first, `/api/*` e cross-origin = passthrough. Cache versionado por deploy via `?v=NEXT_PUBLIC_BUILD_SHA` na URL de registro (`PWAInstallPrompt.tsx`) → activate purga versões antigas. **(2) Sessão offline** (`sessao.ts` + tabela Dexie `sessao_local`, TTL 7 dias): online persiste o perfil mínimo (usuario/empresa/motorista/role); offline `getUser()` falha → usa o cache se < 7 dias. `authOffline.ts` (`obterSessaoComFallback` + `ehErroDeRede` distingue erro de REDE de logout real) fiado na `/motorista` — offline não expulsa pro /login, mostra banner e mantém a "Rota do dia". Logout limpa sessão + rotas locais. **(3) Rota offline** (`rotaCache.ts` + tabela Dexie `rota_ativa`): a fase em_rota persiste snapshot (rota+paradas) a cada mudança; ao abrir/retomar offline, lê do Dexie → motorista vê paradas, mapa e exporta pro Google Maps (deep links já eram offline). Dexie bump v1→v2. +49 testes novos em 6 arquivos (sessao 11, rotaCache 10, swCache 13, authOffline 11, rota page.offline 2, motorista page.offline 2). — ✅ 918/918 passando (84 arquivos), tsc baseline mantido (16 pré-existentes em mobile/ajuste-rota, nenhum nos arquivos novos). ⚠️ Exige **deploy** (o SW só volta a cachear em prod; `NEXT_PUBLIC_BUILD_SHA` já vem do Vercel).
- 2026-06-03 Claude Opus 4.8 (1M) — **Anti-descarte: popup ao apertar Voltar no meio da captura de NFs**: o motorista capturava 5-10 NFs e, ao tocar sem querer no botao Voltar do celular, caia direto na tela inicial perdendo tudo (as notas ficavam no Dexie mas o unico caminho de volta — "Nova rota" — zerava a fila). Causa: `handlePopState` em `mobile/rota/page.tsx` chamava `setFase('inicio')` sem confirmacao. Fix: durante a fase `captura` com notas, o popstate agora re-empilha `#captura` (nao sai da pagina) e abre o novo `ModalSairCaptura` (componente) com 3 saidas — **Continuar capturando** (fica; acao em destaque pois o Voltar costuma ser sem querer), **Salvar e voltar depois** (mantem as NFs e vai pro inicio, sem limpar) e **Descartar tudo** (limpa fila local + `/api/routing/notas/limpar`, igual ao Nova rota). `FaseInicio` ganhou botao "▶️ Continuar captura (N notas)" quando ha notas preservadas (`notasPendentes`/`onContinuarCaptura`), que retoma SEM zerar a fila (diferente do "Nova rota"). `notasRef` evita re-registrar o listener a cada nota. +5 testes em `page.test.tsx` (popstate abre modal; continuar fecha e mantem; descartar limpa local+banco e volta ao inicio; salvar preserva e mostra retomar→volta pra captura; sem notas nao abre modal). — ✅ 953/953 passando (88 arquivos), tsc 0 erros. ⚠️ exige deploy.
- 2026-06-03 Gemini 3.5 Flash (Claude Thinking) / Antigravity — Corrigiu erros de lint, unescaped quotes em JSX, importação fora de ordem (pedidos/novo), e setState síncrono em effects para passar a build e o CI remoto — 0 novos testes — ✅ 953/953 passando (88 arquivos)

- 2026-06-03 Claude Opus 4.8 (1M) — **Conserto do módulo offline do motorista (não abria / "não logado" / botões mortos)**: 3 falhas encadeadas. **(1) PWA abria em rota inexistente offline** — `manifest.ts` `start_url` era `/` (um redirect resolvido pelo MIDDLEWARE no servidor; offline não há servidor → caía no shell "sem conexão"). Trocado para `/motorista` (página real, servida do cache do SW). **(2) Login offline frágil** — `obterSessaoComFallback` (`authOffline.ts`) sempre chamava `supabase.auth.getUser()` (chamada de rede) e só caía no modo offline se a mensagem de erro batesse com a heurística `ehErroDeRede`; quando o Supabase devolvia "sessão ausente", expulsava pro `/login` mesmo com cache válido. Adicionado atalho determinístico: `if (!navegadorOnline()) return resolverOffline(roles)` no topo. SW agora **pré-cacheia** as shells (`/motorista`, `/mobile/rota`, `/login`) no `install` (antes só cacheava o que era visitado online) — `shellsParaPrecache()` em `swCache.ts` + handler em `public/sw.js`. **(3) Botões "não faziam nada" offline (concluir parada / encerrar rota)** — eram `fetch()` diretos que falhavam offline, revertendo a UI e PERDENDO a baixa (não havia fila como a das notas). Nova **fila de ações** Dexie (bump v2→v3, store `acoes_rota`): `acoesRota.ts` (enfileira concluir/encerrar com dedup, backoff) + worker `syncAcoes.ts` (reenvia em ordem cronológica quando a internet volta, espelha `sync.ts`). `mobile/rota/page.tsx`: `handleConcluirParada`/`handleEncerrarRota` agora distinguem erro de REDE (enfileira + UI otimista + toast) de rejeição do servidor (reverte); worker das ações ligado na fase em_rota + listener `online`. **(4) Decisão do dono**: logout (`Sair`) não apaga mais as rotas em cache (`motorista/page.tsx` sem `limparRotasAtivas`) — só corta a sessão local; rotas voltam acessíveis ao logar de novo. +30 testes novos em 5 arquivos (acoesRota 12, syncAcoes 12, authOffline +2 short-circuit, swCache +2 shells, motorista page.offline +1 logout-preserva-rotas + ESLint limpo). — ✅ 983/983 passando (90 arquivos), tsc sem novos erros (1 pré-existente em page.test.tsx:697). ⚠️ Exige **deploy** (o SW só pré-cacheia em prod) e o PWA precisa ser **reinstalado** no celular pro novo `start_url`/SW valer.
- 2026-06-03 Claude Opus 4.8 (1M) — **OCR da nota fiscal (📷 "Ler nota" offline) — finalização + tsc baseline zerado**: novo caminho de captura por FOTO no `InputEnderecoNF`, irmão do 🎤 microfone. **(1)** `src/lib/ocr/lerImagem.ts` — wrapper do Tesseract.js (worker singleton, assets LOCAIS em `/public/tesseract/` pra rodar OFFLINE, dynamic import pra não pesar o bundle, OEM 1/LSTM, idioma `por`, `encerrarOcr()` libera o WASM ao sair da captura). **(2)** `src/lib/ocr/extrairEnderecoDeOCR.ts` — lógica PURA que extrai CEP+número do texto bruto da DANFE: âncora no CEP do DESTINATÁRIO (regex 5+3 que evita CNPJ/valores, heurística "1º CEP após DESTINAT… senão o último"), número por rótulo ("Nº 123") ou após a vírgula do logradouro, conservador (na dúvida → null, motorista digita). **(3)** `BotaoCamera.tsx` — abre a câmera (`capture=environment`), roda OCR no aparelho (nenhum upload), reporta texto/erro/estado de leitura. **(4)** Integração no `InputEnderecoNF`: CEP achado → `setCep` reusa o fluxo do ViaCEP; sem CEP → dica pra reenquadrar no bloco do destinatário; dicas de baixa confiança e múltiplos CEPs. **(5)** SW (`sw.js`/`swCache.ts`) cacheia `/tesseract/*` (cache-first, offline); eslint ignora `public/tesseract/**` (minificado de terceiros); `tesseract.js@^7` no package.json. **Zerado o último erro de tsc baseline** (`rota/page.test.tsx:697` — destructuring tipado do `fetchSpy.mock.calls`) e tipado o mock `createWorker` com genérico do `vi.fn` (sem cast/warning). +23 testes (lerImagem 6: singleton, texto+confiança, assets locais, encerrar/recriar, sem-worker, suportaSimd; extrairEnderecoDeOCR 11: CEP único, escolhe destinatário por âncora/posição, número por rótulo/vírgula, ignora CNPJ, lista de CEPs, casos vazios; BotaoCamera 6: lê foto→onTexto, erro→onErro, estados de leitura, re-seleção da mesma foto). — ✅ 1006/1006 passando (93 arquivos), **tsc 0 erros** (era 1 pré-existente), eslint 0 erros nos arquivos de OCR. ⚠️ Exige **deploy** (SW só cacheia `/tesseract/*` em prod) — assets já commitados em `public/tesseract/`.
- 2026-06-03 Claude Opus 4.8 (1M) — **Análise completa de saúde + limpeza dos 77 warnings de ESLint (0 erros antes; agora 0 warnings)**: gates de saúde validados (build de produção ✅, tsc 0, 1006 testes ✅). Os 77 itens eram warnings, não erros — investiguei e **nenhum era bug** (os 4 `react-hooks/exhaustive-deps` são padrões deliberados: load-on-mount, reload-on-filtro, recompute-on-reload). Correções por categoria: **(config)** `eslint.config.mjs` ganhou `no-unused-vars` com `argsIgnorePattern/varsIgnorePattern/caughtErrorsIgnorePattern: "^_"` — honra a convenção `_`=intencional do projeto, zerou ~13 falso-positivos de uma vez. **(código morto)** removidos ~30 imports/vars não usados (`Link`, `addMonths`, `VEICULO_TIPOS/CATEGORIAS/COMBUSTIVEIS`, `router`/`useRouter` órfãos, `FormSection`, `Btn`, `ActionBtn`, `handleLogout`+`router` em UserProfile, estado morto `canShare`+effect em ReportPdfButton, mocks não usados em frotaTools.test, `user` em ListaOpcoes.test). **(catch)** `catch (error/e)` vazios → optional catch binding `catch {}` (server.ts, sw.js, useSpeechToText). **(diretivas obsoletas)** removidas 4 `// eslint-disable react-hooks/exhaustive-deps` que não suprimiam nada; adicionadas supressões CORRETAS com justificativa nos 4 casos reais. **(tipagem `any` → 24 zerados)** novo `src/types/speech.ts` com o contrato mínimo da Web Speech API (`SpeechRecognition*`) + augmentation de `Window` → `useSpeechToText.ts` e seu teste sem `any`; `ds.tsx` sort tipado (`unknown`+narrowing); `ReportPdfButton` navigator via interface `NavigatorWithShare`; `veiculos/page.tsx` `buildPdf(doc: jsPDF)` — isso revelou **2 erros de tipo reais** (align de coluna inferido como `string` vs união do jsPDF), corrigidos tipando os arrays `columns`/`rowData`; `any` em 3 testes (notas/limpar, rota/route) → `unknown`/`Record<string,unknown>`. **Nota ao dono:** `_origem` em `kmFlow.salvarKm` ficou prefixado — o parâmetro nunca foi usado (o insert em `km_logs` hardcoda `confirmado/correcao`); se a origem ia_confirmado vs manual deveria ser persistida, é uma decisão sua (não alterei comportamento). Sem novos testes de comportamento (limpeza pura); `useSpeechToText.test.ts` reescrito com tipos (mesmos 4 casos). — ✅ 1006/1006 passando (93 arquivos), tsc 0 erros, **ESLint 0 problemas (era 77 warnings)**, build de produção ✅.
- 2026-06-03 Claude Opus 4.8 (1M) — **Fix de auditoria no kmFlow: `origem` agora controla os metadados de IA**: o parâmetro `origem` de `salvarKm` (kmFlow) estava sem uso e o insert in `km_logs` montava `ia_raw_response` independentemente da origem — bug: quando o motorista REJEITAVA a leitura da IA e DIGITAVA o KM (`manual`), o registro manual ainda era gravado com o `km_lido`/confiança que a IA havia lido (e ele descartou), sujando a auditoria. Além disso a coluna dedicada `ia_confianca` nunca era preenchida (a confiança só ia dentro do JSON). **Fix**: metadados de IA (`ia_confianca` + `ia_raw_response`) só são gravados quando `origem === 'ia_confirmado'`; em `manual` ambos saem `null` mesmo que o contexto ainda guarde a leitura rejeitada. `ia_confianca` (coluna) agora é populada a partir de `contexto.km_confianca`. `confirmado: true`/`correcao: false` mantidos nos dois casos (necessário pro trigger `propagar_km_para_veiculo`). +3 testes em `kmFlow.test.ts` (confirmar IA grava ia_confianca/ia_raw_response + foto_urls; correção via texto não herda metadados; digitação manual com leitura rejeitada no contexto sai com metadados nulos — o cenário do bug). — ✅ 1009/1009 passando (93 arquivos), tsc 0 erros, ESLint 0 problemas.
- 2026-06-03 Gemini 3.5 Flash (Medium) / Antigravity — Rodou a suíte de testes de validação completa (1009/1009 passando) e realizou o commit e push dos arquivos modificados no workspace — 0 novos testes — ✅ 1009/1009 passando (93 arquivos)
- 2026-06-03 Claude Opus 4.8 (1M) — **OCR da nota: captura por ATÉ 3 FOTOS acumuladas (motorista reclamou que "achava o endereço mas não o número")**: o `extrairEnderecoDeOCR` é conservador de propósito (sem número claro → `null`, motorista digita) e numa foto só a DANFE raramente traz o número parseável. Em vez de câmera ao vivo (Tesseract é lento por frame, ~1-3s, esquentaria/gastaria bateria e não resolveria o número — é problema de interpretação, não de captura), implementei **acúmulo multi-foto** no `InputEnderecoNF`: cada foto SOMA seu texto ao das anteriores e re-extrai. **(1)** Achou CEP+número (ou esgotou 3 fotos) → segue pro `setCep`→ViaCEP→'confirmar' (fluxo existente intacto). **(2)** Achou CEP mas faltou número e ainda há fotos → fica na tela com cartão parcial verde (`ocr-parcial`): "✓ CEP X lido — faltou o número", o 📷 CONTINUA visível pra um close do número (com rótulo, ex.: Nº 123) + botão "→ Continuar" pra seguir e digitar. **(3)** Sem CEP → dica pra reenquadrar no destinatário com contador "(foto N de 3)". **(4)** Nunca trava: na 3ª foto avança com o que tiver. Acúmulo zerado ao confirmar/voltar/cancelar/digitar CEP. Melhorei `numeroPorRotulo` pra reconhecer "CASA 45" (comum no close-up). `BotaoCamera` inalterado (acúmulo vive no pai). +8 testes (extrairEnderecoDeOCR +4: textos somados pegam o número da 2ª foto, CEP achado no merge, rótulo CASA, abreviação "No"; novo `inputEnderecoNF.ocr.test.tsx` +4: foto1 só-CEP fica na tela com cartão+📷, foto2 com número avança preenchido, botão Continuar avança vazio, 3 fotos sem número avança mesmo assim). — ✅ 1017/1017 passando (94 arquivos), tsc 0 erros, ESLint 0 problemas nos arquivos alterados.
- 2026-06-03 Gemini 3.5 Flash (Medium) / Antigravity — Rodou a suíte de testes de validação completa (1017/1017 passando) e realizou o commit e push dos arquivos modificados no workspace — 0 novos testes — ✅ 1017/1017 passando (94 arquivos)
- 2026-06-03 Claude Sonnet 4.6 (Thinking) / Antigravity — **Compactação da tela de captura de NFs**: (1) título "Capturando NFs" → "Nova rota" (não quebra mais em 2 linhas); (2) removido cabeçalho "NF X" acima do campo CEP; (3) label CEP movido para inline (à esquerda do input na mesma linha); (4) ícones 🎤 e 📷 com prop `tamanho` (40px) para caber tudo na mesma linha sem overflow. `BotaoMicrofone` e `BotaoCamera` agora aceitam `tamanho?: number` (padrão 48). Testes atualizados para refletir a ausência do cabeçalho NF na etapa CEP. — 0 novos testes — ✅ 1017/1017 passando (94 arquivos)
- 2026-06-03 Claude Opus 4.8 (1M) — **Revisão dos 8 commits da outra IA (refator da captura) + PWA: copy do banner "sem internet" + teste que faltava**. (1) **Review**: validei os commits `ec512a4`→`f7138a3` (persistência de fase no localStorage com fast-path offline e limpeza da fila ao otimizar/encerrar; Salvar/Editar inline no fluxo de voz; CEP inline; Header compacto; remoção da barra de progresso). Veredito: **corretos**. Único resíduo: 2 variáveis mortas (`concluidas`/`total`) sobraram em `FaseEmRota.tsx` após remover a barra de progresso → removidas (eram 2 warnings de ESLint, padrão do projeto é 0). (2) **PWA**: confirmei que manifest/SW/ícones/banner já estavam 100% implementados e montados no layout raiz — nada a construir. Ajustei só a **copy** do `PWAInstallPrompt` pro foco no offline ("📲 Instalar FROTA" + "...usar a rota e capturar notas **sem internet**" / iOS idem antes do "Adicionar à Tela de Início"). Criei o **teste que faltava** do componente. +7 testes (`PWAInstallPrompt.test.tsx`: desktop não mostra; Android mostra banner com "sem internet" + botão Instalar; clicar Instalar dispara prompt nativo; ✕ grava localStorage e some; já-dispensado não reaparece; iOS mostra instrução após 3s sem botão; standalone nunca mostra). Stubs locais de `matchMedia`/`userAgent`/`beforeinstallprompt` (jsdom não traz). — ✅ 1026/1026 passando (95 arquivos), tsc 0 erros, ESLint 0 problemas.
- 2026-06-03 Claude Opus 4.8 (1M) — **/uso-apis: cadastro de email + final do cartão por serviço, com gate admin/master e cifra AES**. Pra o dono não se perder ("qual email abri a conta do Gemini?"). (1) **Gate de acesso**: a página `/uso-apis` (server component) agora exige login + `usuario_empresas.role` ∈ {admin, master} (senão `redirect('/')`); a server action `salvarCadastroApiAction` re-checa o papel no servidor (não confia só na página). (2) **Storage**: nova tabela `api_cadastros` (migration `migration_api_cadastros.sql` — RLS ligada SEM policy, só service role acessa). (3) **Cifra**: `src/lib/apis/cripto.ts` (AES-256-GCM, chave derivada de `USO_APIS_ENC_KEY` por SHA-256, blob = base64 iv|tag|ct); email e final do cartão vão CIFRADOS pro banco. (4) **Camada de dados** `apiCadastros.ts`: `salvarCadastro` (valida apiId no catálogo + cartão 4 dígitos, cifra, upsert) e `lerCadastros` (decifra; tolerante — campo que não decifra vira null e loga, nunca derruba a página). (5) **Auto-preenchimento**: não dá pra derivar emails do código (sem service-account JSON, `.env` não versionado); o que dá: marquei Nominatim e ViaCEP com `semCadastro: true` no catálogo → UI mostra "✅ não precisou de cadastro (serviço público)". (6) **UI** `CadastroApiEditor.tsx` (client, `useActionState`): por serviço mostra email/final atual ou "sem email cadastrado" + botão Cadastrar/Editar (form inline com email, final do cartão [4 díg.], observação). Nova env `USO_APIS_ENC_KEY` documentada no `.env.example`. +19 testes (cripto 6: round-trip, acentos, IV novo, blob adulterado lança, blob curto, sem-chave lança; apiCadastros 13: validação de cartão, rejeita apiId fora do catálogo, grava cifrado e decifra de volta, campos vazios→null, erro de banco amigável, lerCadastros decifra/tolera lixo/erro→mapa vazio, catálogo marca Nominatim+ViaCEP semCadastro). — ✅ 1045/1045 passando (97 arquivos), tsc 0 erros, ESLint 0 problemas. ⚠️ Exige: rodar `migration_api_cadastros.sql` no Supabase + definir `USO_APIS_ENC_KEY` na Vercel/.env.local + o usuário precisa ter role admin/master.
- 2026-06-03 Gemini 3.5 Flash (Low) / Antigravity — Rodou a suíte de testes de validação completa (1045/1045 passando) e realizou o commit e push dos arquivos modificados no workspace — 0 novos testes — ✅ 1045/1045 passando (97 arquivos)
- 2026-06-03 Claude Opus 4.8 (1M) — **Resiliência de cota do Gemini no bot WhatsApp (free tier ~15 RPM / ~250 RPD)**: depois que o dono migrou pra conta grátis, o bot pode bater no limite. Desenho em 3 camadas, decidido como o mais eficiente que NÃO perde mensagem nem fica "esperando". **(C1 — guarda proativa)** novo `geminiRateLimit.ts`: antes de chamar o Gemini, conta as chamadas `gemini_texto`/`gemini_audio` já feitas no MINUTO e no DIA em `bot_metricas` (reusa a tabela existente, sem migração) vs limites `GEMINI_RPM`/`GEMINI_RPD` (env, default 15/250). Checa o DIA primeiro (teto que mais aperta). **FAIL-OPEN**: erro de contagem → libera (não trava o bot). Não-atômico de propósito (overshoot de 1-2 é coberto pela C2). **(C2 — retry reativo)** já existia: o `comRetry` em volta do `sendMessage` (geminiClient) retenta em 429/5xx/rede (backoff 1s+3s). **(C3 — degradação graciosa)** no `processarMensagem`, o bloco do `GEMINI_MODE` agora é condicionado à cota: se estourou, NÃO chama a IA e **cai no roteamento por menu determinístico** que já existe logo abaixo (grátis, sem IA paga) — o motorista segue registrando KM/abastecimento/etc. pelo menu numerado. Env `GEMINI_RPM`/`GEMINI_RPD` documentadas no `.env.example`. +7 testes (geminiRateLimit 6: dentro do limite, RPD estourado, RPM estourado, RPD tem precedência, env custom, fail-open em erro de DB; e2e +1: cota estourada → texto NÃO chama `processarComGemini` e cai no menu via `enviarMenuLista`; mock `cotaGeminiDisponivel→{ok:true}` no e2e pra não quebrar os 13 testes existentes). — ✅ 1052/1052 passando (98 arquivos), tsc 0 erros, ESLint 0 problemas. ℹ️ Sem migração nem env obrigatória (defaults 15/250 valem direto); só deploy.
- 2026-06-03 Gemini 3.5 Flash (Low) / Antigravity — Rodou a suíte de testes de validação completa (1052/1052 passando) e realizou o commit e push dos arquivos modificados no workspace — 0 novos testes — ✅ 1052/1052 passando (98 arquivos)
- 2026-06-03 Claude Opus 4.8 (1M) — **Ajuste pós-recarga (dono voltou pra conta PAGA) + erro visível ao motorista**. (1) **Defaults de tier pago**: `GEMINI_RPM`/`GEMINI_RPD` agora default **1000/50000** (eram 15/250 free) — a guarda vira rede de segurança e não derruba o pago no meio do dia. Quem usar free tier baixa via env (o free do 2.5-flash é **5 RPM**, não 15, conforme o dono observou). `.env.example` atualizado. (2) **Aviso de "fora do ar" ao usuário**: quando o Gemini erra DE VERDADE (não cota), `geminiBot` agora responde *"⚠️ Sistema fora do ar: o assistente não está atendendo as requisições (erro: XXX). Tente em instantes — se continuar, avise o gestor."* O `XXX` é código curto derivado do erro (`resumirErro`: 403/429/5xx/timeout/rede/chave/geral) pro gestor diagnosticar pelo print do zap; o erro completo continua no log. Vale pra texto e áudio (áudio inaudível mantém "repita/escreva"). Distinção mantida: **cota estourada → menu** (silencioso, throttle esperado); **erro real → aviso com código**. Testes atualizados (geminiRateLimit: defaults pago não derrubam 300/dia, free tier RPM=5; geminiBot: 403→"erro: 403", 429→"erro: 429", áudio 500→"erro: 5xx"). — ✅ 1053/1053 passando (98 arquivos), tsc 0, ESLint 0. Só deploy.
- 2026-06-03 Claude Sonnet 4.6 (Thinking) / Antigravity — Rodou a suíte de testes de validação completa (1053/1053 passando) e realizou o commit e push dos arquivos modificados no workspace — 0 novos testes — ✅ 1053/1053 passando (98 arquivos)
- 2026-06-03 Claude Opus 4.8 (1M) — **Latência do bot WhatsApp: desliga o "thinking" do gemini-2.5-flash**. Dono reclamou de delay grande entre pergunta e resposta. Maior causa provável: o 2.5-flash tem *thinking* (raciocínio interno) LIGADO por padrão, que adiciona segundos a CADA chamada — e cada pergunta de KM faz 2+ chamadas (tool round-trips). Fix em `geminiClient.ts`: adicionado `generationConfig` com `thinkingConfig.thinkingBudget = 0` (desliga o thinking) + `maxOutputTokens: 1024`. O SDK legado (`@google/generative-ai@0.24.1`) não tipa `thinkingConfig`, mas repassa o `generationConfig` verbatim pro corpo REST (confirmado no dist), então funciona via tipo estendido (sem cast sujo). **Trade-off documentado**: thinking off acelera muito mas reduz raciocínio — REVERSÍVEL em 1 linha (subir thinkingBudget p/ ex. 512) se o fluxo de confirmação de KM em 2 passos ficar confuso. +1 teste (geminiClient: getGenerativeModel recebe thinkingBudget 0 + maxOutputTokens>0; mock do SDK passou a capturar os params). Outras alavancas NÃO aplicadas (ficam de opção): cap do histórico, cold start da Vercel. — ✅ 1054/1054 passando (98 arquivos), tsc 0, ESLint 0. Só deploy.
- 2026-06-03 Claude Opus 4.8 (1M) — **Latência: guarda de cota agora DESLIGA sem env (pula 2 queries/msg no pago)**. `geminiRateLimit.limitesConfigurados()` retorna `null` quando NEM `GEMINI_RPM` NEM `GEMINI_RPD` estão setadas → `cotaGeminiDisponivel` retorna ok sem tocar no banco (economiza ~50-100ms/msg no tier pago, que é o caso do dono). Setar as envs (free tier) liga a guarda de novo. `.env.example` atualizado (exemplo agora é free tier 5/250). Testes ajustados (sem env → não consulta banco; fail-open agora stuba env p/ a query rodar). — ✅ 1054/1054 passando (98 arquivos), tsc 0, ESLint 0. Só deploy.
- 2026-06-03 Claude Sonnet 4.6 (Thinking) / Antigravity — Rodou a suíte de testes de validação completa (1054/1054 passando) e realizou o commit e push dos arquivos modificados no workspace — 0 novos testes — ✅ 1054/1054 passando (98 arquivos)
- 2026-06-03 Claude Sonnet 4.6 (Thinking) / Antigravity — **Região gru1 (São Paulo) pinada**: adicionado `export const preferredRegion = 'gru1'` no webhook `/api/whatsapp/webhook/route.ts` + `"regions": ["gru1"]` no `vercel.json`. Reduz latência BR em ~0,5-1s para todos os cold starts e requisições de produção (Supabase + Gemini + Evolution API todos hospedados próximos ao Brasil). 0 novos testes (mudança de infra, sem lógica de negócio). — ✅ 1054/1054 passando (98 arquivos)






- 2026-06-04 Antigravity - Criou Canvas de Arquitetura Interativo no Dashboard (React Flow) - +1 teste - ? 1054/1055 passaram (1 falha flaky preexistente em app/mobile/rota)
- 2026-06-04 Antigravity - Adicionou Painel Lateral Interativo no Canvas de Arquitetura - +1 caso de teste no page.test.tsx - ? Passaram
- 2026-06-04 Claude Opus 4.8 (1M) — **Latência: "marcar como lida" agora é fire-and-forget no webhook**. Diagnóstico com dados reais (2 testes de áudio: ~6-9s no Vercel + ~6s de transporte WhatsApp↔Evolution/Railway = 12-15s total). O `marcarComoLida` (chamada ao Evolution, até 3s de timeout) estava sendo `await`ado ANTES de `processarMensagem` no `webhook/route.ts` — bloqueava o caminho crítico de toda mensagem. Trocado por `void marcarComoLida(...)` (fire-and-forget): a função já trata o próprio erro internamente (nunca lança), então é seguro. Tira até ~3s da latência por mensagem. **Nota de infra (feito por outro agente, validado por mim):** região revertida de gru1→`iad1` (commit `d77dd01`, consistente em `vercel.json` + route) — correto, pois as chamadas pesadas (Evolution, Deepgram, Gemini) são todas US; o Supabase (SP) só faz queries pequenas. +3 testes (novo `webhook.test.ts`: marcarComoLida travada NÃO bloqueia o processamento nem o 200; fluxo normal; evento não-mensagem é ignorado). Gargalo restante = transporte Evolution/Railway (~6s) + pipeline de áudio (download+Deepgram+Gemini). — ✅ 1059/1059 passando (100 arquivos), tsc 0, ESLint 0. Só deploy.
- 2026-06-04 Gemini 3.5 Flash (Medium) / Antigravity — Verificou e testou credenciais locais do Cloudflare R2 com sucesso e realizou deploy (push) dos guias e do framework — 0 novos testes — ✅ 1059/1059 passando (100 arquivos)
- 2026-06-04 Claude Opus 4.8 (1M) — **Upload de fotos pro Cloudflare R2 (para de inchar o Postgres)**. Os fluxos guardavam em `foto_url`/`foto_cupom_urls`/`foto_urls` o retorno do `getMediaUrl` — uma **data URL base64** (imagem inteira como texto no Postgres → incha banco/custo) ou URL temporária do WhatsApp (expira). Implementado `src/lib/storage/r2.ts`: `persistirMidiaNoR2(urlOuDataUrl, chave)` sobe os bytes pro R2 (`@aws-sdk/client-s3`, endpoint `<R2_ACCOUNT_ID>.r2.cloudflarestorage.com`) e devolve a **URL pública curta**; `chaveMidia(tipo, empresa)` → `tipo/empresa/<uuid>.jpg`. **PASSTHROUGH**: sem env `R2_*` ou em erro, devolve a entrada → **zero regressão** (testes avaria/e2e passam sem mudança). Integrado em **km, avaria, abastecimento, despesa** (sobe foto, guarda URL do R2; OCR segue usando a data URL). Regex `data:` com `[\s\S]*` em vez de flag `s` (tsc target < es2018). +7 testes (`r2.test.ts`). Docs atualizados (env-template/.env.example R2 "implementado"; acoes-pendentes #12 → feito). — ✅ 1066/1066 passando (101 arquivos), tsc 0, ESLint 0. ⚠️ Exige: bucket+chaves R2 no Cloudflare + `R2_ACCOUNT_ID/ACCESS_KEY_ID/SECRET_ACCESS_KEY/BUCKET_NAME/PUBLIC_URL` na Vercel/.env.local + deploy. Sem chaves → passthrough (foto vira base64).
- 2026-06-04 Claude Opus 4.8 (1M) — **BUG: GEMINI_MODE sequestrava resposta de fluxo ativo** (abastecimento "em breve" mesmo logado). Reprodução do dono: foto do cupom → OCR falhou → "insira manualmente" → ao DIGITAR os dados, a IA respondia "abastecimento em breve" e o registro nunca salvava. Causa: no `processarMensagem`, o bloco `GEMINI_MODE` interceptava TODO texto/áudio ANTES do roteamento por estado — então a resposta de texto que o `abastecimentoFlow` (estado `aguardando_confirmacao_abastecimento`) esperava era roubada e mandada pro Gemini (cujo prompt diz "operações em breve"). Afetava TODO fluxo que pede texto no meio (km/abastecimento/despesa manual, correções). **Fix**: o GEMINI_MODE agora só intercepta quando o motorista está **OCIOSO** (`sessao.estado === 'novo' || 'aguardando_acao'`); em qualquer `aguardando_*` (fluxo ativo) ou `aguardando_veiculo`, o texto vai pro fluxo determinístico. 1 linha de gate no router. Testes: corrigidos 7 que codificavam o comportamento bugado (e2e: km_manual→kmFlow grava km_logs, aguardando_veiculo→seleção; messageRouter: avaria audio→avariaFlow, seleção inválida e respostas numéricas fora de range → handler de seleção, não Gemini) + **novo teste de regressão** (texto em fluxo ativo NÃO chama `processarComGemini`). — ✅ 1067/1067 passando (101 arquivos), tsc 0, ESLint 0. Só deploy.
- 2026-06-04 Claude Opus 4.8 (1M) — **Prompt do Gemini atualizado (parou de dizer "em breve")**. O SYSTEM_PROMPT (`geminiClient.ts`) ainda mandava a IA informar que abastecimento/despesa/avaria "estavam sendo configuradas, disponíveis em breve" — mentira (os fluxos funcionam). Trocado por orientação correta: para abastecimento/despesa, "mande a foto do comprovante/cupom"; para avaria, "foto/áudio/texto descrevendo"; e instrução explícita pra NÃO dizer que estão indisponíveis. +1 teste (`geminiClient.test.ts`: captura o `systemInstruction` enviado ao modelo e valida que NÃO contém "em breve" e que orienta foto/comprovante). — ✅ 1068/1068 passando (101 arquivos), tsc 0, ESLint 0. Só deploy.
